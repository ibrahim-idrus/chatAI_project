{
  "name": "@chatai/db",
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "node -e \"console.log('@chatai/db: no dev server (use db:push/db:migrate)')\"",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "push": "drizzle-kit push"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "dotenv": "^16.6.1",
    "drizzle-kit": "^0.31.10",
    "postgres": "^3.4.9",
    "typescript": "~6.0.2"
  }
}
